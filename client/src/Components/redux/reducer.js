import { ALL_RECIPES } from "./actionTypes";

const initialState={
    recipes:[]
}

function reducerReciper(state=initialState,{type,payload}) {
    switch (type) {
        case ALL_RECIPES:
            
            break;
    
        default:
            break;
    }
}

export default reducerReciper